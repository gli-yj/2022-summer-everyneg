
function ranAudioID() {
  return math.random()* data.length
  //not sure if we use data.length-1 or not

}

somefunction dataintoarray() {
//we want to put the 5 audio files in some folder into an array
}

someaudiofilecall () {
//userandAudioID to call a specific place in the array and save that as a variable that we can call in the list

}
